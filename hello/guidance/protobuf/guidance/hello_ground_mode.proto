syntax = "proto3";

package Guidance.HelloGroundMode.Messages;

// Ground mode configuration
message Config {
    bool say = 1;
}

// Union of all possible events of this package.
message Event {
    oneof id {
        uint32 count = 1;
    }
}
