syntax = "proto3";

package parrot.missions.samples.hello.guidance.messages;

// Ground mode configuration
message Config {
    bool say = 1;
}

// Union of all possible events of this package.
message Event {
    oneof id {
        uint32 count = 1;
    }
}
